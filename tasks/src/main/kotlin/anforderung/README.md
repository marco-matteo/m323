# Aufgabe 2.1
## Eine Reise Planen:

```
Route -> List of Destinations
Destination -> String like "Zurich"

newRoute(destination) -> Route -> ["Zurich"]

addDestination(route, destination) -> Route -> ["Basel", "Zurich"]
```